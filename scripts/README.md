# scripts/

Dev and ops scripts for the loombox monorepo (SPEC §10.1). Empty for now; things
like the relay start/deploy helper and the headless v0 end-to-end harness land
here as their issues are built.
